-> First Step in CMD
pip install selenium pyodbc webdriver_manager 

-> To run the application in CMD in the same location
python "Start Sending Message.py"

-> To create .exe file in CMD in the same location
pip install pyinstaller
python -m PyInstaller "Start Sending Message.py"